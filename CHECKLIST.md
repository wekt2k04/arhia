# Checklist de suivi — AGIRH V8

*Mis à jour à la fin de chaque milestone. Statuts : ✅ **FAIT** · 🔄 **EN COURS** · 🔁 **EN CONTINU** · ⬜ **À FAIRE** · 🚧 **BLOQUÉ**. Observations concises — ce qui a été livré, ce qui manque, tout écart par rapport à `LOGIQUE_METIER.md`/`STACK_TECHNIQUE.md`/`ARCHITECTURE.md`.*

| # | Milestone | Statut | Livré | Reste à faire | Observations |
|---|---|---|---|---|---|
| 0 | Cadrage — Logique métier | ✅ | `LOGIQUE_METIER.md` : rôles, workflows Onboarding/Offboarding, RBAC, garde-fous IA | — | Basé sur les 2 checklists SMSI réelles (anonymisées). 3 cas particuliers (§8) restent des propositions à valider en pratique. |
| 1 | Cadrage — Stack technique | ✅ | `STACK_TECHNIQUE.md` : .NET+Next.js, JWT+Identity, SQL Server+Qdrant, SSE, Docker Compose local, logs technique+audit séparés, pipeline RAG 4 phases (ONNX embedding/reranking), Ollama 2 modèles (routeur+générateur) | — | Noms/tailles précis des modèles Ollama et schéma exact des logs restent ouverts (§10 du document). |
| 2 | Cadrage — Architecture (hexagonal, dossiers, diagrammes) | ✅ | `ARCHITECTURE.md` : arborescence cible, 4 diagrammes Mermaid (couches, création onboarding, circuit de validation, RAG+chat, RBAC) | — | Structure de dossiers pas encore créée sur le disque — c'est la cible du milestone 3. |
| 3 | Socle métier (modèle de données, workflow engine, RBAC 3 rôles) | ✅ | `Agirh.Domain` (Pole, CompteUtilisateur, Collaborateur, WorkflowTemplate/Section/Item, WorkflowInstance/ChecklistItemStatus, Matricule VO), `Agirh.Core` (7 ports, RbacMatrix, PoleScopeGuard, AccesRefuseException, 11 use cases) | — | Circuit de validation de template, portée RH=pôle, archivage lecture seule tous testés. Les 3 cas particuliers (§8 LOGIQUE_METIER) ont des primitives (Annuler/Suspendre/Reprendre, ChangerDePole) mais pas de use case dédié — comportement encore "à valider". |
| 4 | Authentification (JWT + ASP.NET Identity) | ✅ | `Agirh.Infrastructure` (AgirhDbContext + 5 configs EF + 5 repositories + AspNetIdentityPasswordHasher + JwtTokenGenerator), `Agirh.Api` (Program.cs, AuthController : register/login/me/elever-role, CurrentUserAccessor). Migration `InitialCreate` **appliquée sur SQL Server réel** (conteneur `agirh-sql` remis à zéro), flux register→login→me→mauvais-mot-de-passe **vérifié en HTTP réel** (200/200/200/401) | — | Auto-inscription → rôle Collaborateur forcé (jamais élevé à l'inscription). Élévation de rôle réservée à Admin/Qualité (RBAC vérifié). `role` sérialisé en entier JSON (0=Collaborateur) — lisibilité à améliorer plus tard (`JsonStringEnumConverter`), non bloquant. |
| 5 | Circuit qualité de validation des templates | ✅ | Use cases + EF mapping (milestones 3-4) + `TemplateController` (`POST api/templates`, `.../verifier`, `.../approuver`, `.../rejeter`) — vérifié en HTTP réel (routes protégées, 401 sans auth) | — | Pas un jalon isolé au final — absorbé par 3, 4 et 6. |
| 6 | Frontend agent-first (page de garde, chat, notifications SSE) | ✅ | **Complet et vérifié en HTTP réel de bout en bout** (Api .NET + serveur Next.js réellement démarrés, pas de mock). Page de garde publique, auth BFF (cookie httpOnly, JWT jamais exposé au client — confirmé), pages `/login`/`/register`. **Chat réel** : `app/api/chat/demander` (proxy SSE pur, `response.body` transmis tel quel) + `ChatWidget` (`EventSource`, fragments accumulés en direct, rendu markdown via `react-markdown`/`@tailwindcss/typography`, sources affichées à la fin). **Notifications réelles** : `app/api/notifications/stream` + `NotificationBar` (même pattern). Backend API entièrement prêt en amont (tous les endpoints CRUD + SSE chat/notifications). **UX/UI retravaillée après coup (2026-08-16, session cloud)** : composants shadcn/ui (style "new-york", basés sur Radix, écrits à la main — CLI shadcn injoignable dans ce réseau restreint, `ui.shadcn.com` bloqué par la policy sandbox) ajoutés dans `frontend/components/ui/` ; identité visuelle indigo (CSS variables `app/globals.css`) sur les 4 pages ; police Geist effectivement appliquée (`tailwind.config.ts` `fontFamily`, oubliée au scaffold initial) ; chat : avatars, indicateur de streaming, sources en badges, auto-scroll, `ScrollArea` ; accessibilité : `lang="fr"` (était `en`), `role="status"`/`aria-live` sur le flux de streaming et la barre de notifications, labels `htmlFor`/`id` explicites sur les formulaires. `npm run build`/`lint`/`tsc --noEmit` verts ; vérifié visuellement (Playwright, desktop+mobile) sur page de garde/login/register — chat non vérifiable dans cette session (pas de Docker dans ce sandbox, donc pas de backend/session réelle pour l'atteindre). | Vérification visuelle réelle du chat (avatars, streaming, sources) encore à faire avec le backend démarré. | Next.js 16/Tailwind v4 (defaults `create-next-app@latest`) exigent Node ≥20, cette machine a Node 18.20 — épinglé à Next 15/Tailwind v3 (le pin a aussi contraint le choix shadcn : composants écrits pour Tailwind v3 classique, pas le moteur v4). Bug réel trouvé et corrigé : la compression intégrée de `next start` bufferisait tout le flux SSE avant de l'envoyer (0 octet avant la fin complète) — corrigé avec `compress: false` dans `next.config.ts`, reréifié : fragments bien progressifs après coup. `TaskStop` ne tue pas toujours le process `node` enfant sur Windows (vérifié 3 fois) — vérifier `Get-Process node` si un build échoue avec une erreur de verrou de fichier. Suite de tests .NET complète pas reconfirmée dans son ensemble depuis plusieurs checkpoints (instabilité d'environnement) — sous-ensembles ciblés tous verts. Endpoints de lecture/liste (ex. "mes collaborateurs") et routeur conversationnel (~27% de mauvais routage) restent des chantiers séparés, hors périmètre de ce milestone. |
| 7 | Pipeline RAG (chunking → embedding ONNX → Qdrant → reranking ONNX) | ✅ | **4 phases complètes, vérifiées contre de vraies infrastructures** (pas de mocks) : `MarkdownChunker` (structurel + recouvrement, bug boundary trouvé et corrigé), `XlmRobertaTokenizer` (correction d'offset SentencePiece→Hugging Face vérifiée empiriquement, encodage simple + encodage paire pour le reranking), `OnnxEmbeddingAdapter` (mean-pooling + L2, 768d), `QdrantVectorSearchAdapter` (index + recherche cosinus), `OnnxRerankerAdapter` (cross-encoder, format RoBERTa `<s>requête</s></s>document</s>`, sigmoïde). Test capstone : chunking d'un vrai document du corpus → embedding → indexation Qdrant → recherche → reranking → le bon chunk ressort en tête | Câblage dans un use case Core — fait au milestone 8 | Modèles ONNX (`Xenova/paraphrase-multilingual-mpnet-base-v2` + `onnx-community/bge-reranker-v2-m3-ONNX`, quantifiés int8, ~850 Mo total) + Qdrant (conteneur `agirh-qdrant`) — voir `scripts/download-models.ps1`, jamais commités. |
| 8 | Orchestration conversationnelle (Router + Generator) | ✅ | `RepondreConversationUseCase` (Router→RAG-ou-statut→Generator, anti-hallucination en code : aucun appel Generator si 0 candidat ou tout sous le seuil de pertinence), `OllamaRouterAdapter`/`OllamaGeneratorAdapter` (`phi4-mini:3.8b`), `IngererCorpusUseCase` + `MarkdownChunkerAdapter` (corpus réel → Qdrant, RBAC AdminQualite), `ChatController` (`POST api/chat/demander`, refus RBAC renvoyé en message conversationnel plutôt qu'en 403 brut), `AdminController` (`POST api/admin/reindexer-corpus`). **Vérifié en HTTP réel de bout en bout** : réindexation du vrai corpus (6 docs → 72 chunks), question documentaire → réponse sourcée correcte (`04_procedures_it_securite.md`), question hors-périmètre → refus poli, question de statut sans fiche → message gracieux "dossier introuvable". 158/158 tests verts. | — | **Écart stack documenté** : Router et Generator utilisent tous deux `phi4-mini:3.8b` (pas 2 modèles distincts) — `gemma4:12b` testé, >2min sans réponse sur cette machine (pas de GPU) ; décision transparente dans `STACK_TECHNIQUE.md` §5. Seuil de pertinence reranking (0.01) provisoire, à affiner avec le jeu de Q/R gold (milestone 9). |
| 9 | Corpus RAG + jeu de Q/R gold + évaluation | 🔁 | `corpus/` : 6 documents indexés et vérifiés (72 chunks). `eval/gold_qa.json` : 48 questions (36 documentaires, 6 hors périmètre, 6 hors corpus). **Retrieval** (embedding→Qdrant→reranking, sans LLM) : 36/36, fait partie de la suite par défaut (`EvaluationGoldRetrievalTests`). **E2E réel** (Router+RAG+Generator via Ollama) : 1 run complet obtenu = 21/48 (27 échecs), analysé et catégorisé en détail — a mené à 3 corrections livrées (voir observations) + 1 tentative de correction du routeur **testée empiriquement et abandonnée** (n'a résolu qu'1 cas sur 11 revérifiés, sans effet mesurable) | Chiffres post-correction non re-confirmés sur les 48 questions (voir observations) ; calibrage fin du routeur pour les ~13 questions encore mal classées (StatutDossier/HorsPerimetre au lieu de Documentaire) | Documents rédigés nous-même, ancrés sur les 2 checklists SMSI réelles + `LOGIQUE_METIER.md`, anonymisés. **Corrections livrées et vérifiées** (spot-check réel ciblé, pas le run complet) : (1) `Sourcee`/sources ne sont plus renvoyés `true` quand le générateur a en fait refusé en texte (score de reranking élevé ≠ réponse présente — vérifié empiriquement, un chunk hors-sujet a scoré 0.78) ; (2) prompt du générateur clarifié pour réduire les refus injustifiés quand le contexte contient la réponse ; (3) 3 mots-clés du jeu gold trop stricts corrigés. **Tentative abandonnée** : réécriture du prompt routeur (plus d'exemples/règles) pour corriger les ~13 questions mal classées — re-testée sur 11 cas réels après coup, 10/11 inchangés → annulée (`git checkout`), prompt routeur revenu à la version originale. Cause probable : la capacité d'un modèle 3.8B à suivre des règles explicites plafonne, plus d'exemples ne suffit pas. Root cause documentée dans `HANDOFF/NEXT_SESSION.md` pour ne pas retenter la même approche. **Environnement** : de nombreuses tâches d'arrière-plan (5+ min) ont été tuées sans explication cohérente pendant cette session (pas systématiquement liées à la veille) — a empêché une reconfirmation complète des 48 questions après les correctifs. |
| 10 | Tests (xUnit/Moq/FluentAssertions) | 🔁 | **158/158 tests verts**, 0 warning au build (dont `RepondreConversationUseCaseTests` : 9 tests — hors-périmètre sans appel Generator, 0 candidat/sous-seuil → refus sans appel Generator, réponse sourcée, dossier introuvable, statut personnel, portée RH cross-pôle/même-pôle ; `OllamaRouterAdapterTests` : 4 cas de classification réels + repli sûr si injoignable) | Couverture sur chaque milestone livré, cible N/N | Transversal — pas un jalon isolé, qa-executioner mobilisé à chaque étape. |
| 11 | Déploiement démo (Docker Compose local) | ✅ | `docker-compose.yml` : sqlserver + qdrant + api + frontend, chacun avec son Dockerfile (Api : multi-stage .NET, migrations EF Core auto-appliquées au démarrage ; frontend : multi-stage Node 20, Next 15/Tailwind v3 inchangés). Pas de service Ollama conteneurisé — l'Api rejoint l'Ollama natif de l'hôte via `host.docker.internal`. **Vérifié en conditions réelles complètes** : `docker compose up -d --build` sur base fraîche → migration auto → inscription/connexion via le frontend conteneurisé → chat streamé de bout en bout avec la bonne source citée, à travers toute la pile | Bootstrap du premier compte Admin/Qualité + réindexation du corpus restent des étapes manuelles au premier démarrage (mêmes étapes qu'en dev, pas spécifique à Docker) | Secrets (mot de passe SA, clé JWT) via `.env` (jamais commité, voir `.env.example`). |

## Ouvert / en attente
- Temps restant sur le stage et livrables attendus (rapport, soutenance, dépôt, démo live) — jamais communiqué.
- Comportements précis des 3 cas particuliers (LOGIQUE_METIER.md §8).
- Contenu exact du corpus RAG et du jeu de questions/réponses gold.
- Noms définitifs des ~5 pôles/départements.
