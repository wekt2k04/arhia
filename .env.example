# Secrets pour docker-compose.yml. Copier en .env (jamais commité) et remplir avant `docker compose up`.
SQL_SA_PASSWORD=<mot-de-passe-sa-local-fort>
JWT_SIGNING_KEY=<cle-secrete-locale-32-caracteres-minimum-jamais-committee>
