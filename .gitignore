# ── Secrets / config locale ──
.env
.env*.local
appsettings.*.json
!appsettings.json.example

# ── Build artifacts (.NET / Node, si réintroduits plus tard) ──
bin/
obj/
*.user
*.suo
.vs/
node_modules/
.next/
*.tsbuildinfo

# ── Tests / logs ──
TestResults/
coverage/
coverage.*
logs/

# ── Modèles IA (poids ONNX, plusieurs centaines de Mo — jamais commités) ──
models/
qdrant_storage/
