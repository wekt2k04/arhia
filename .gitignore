# ── .NET build artifacts ──
bin/
obj/
*.user
*.suo
.vs/
TestResults/

# ── Secrets / config locale ──
.env
.env*.local
appsettings.json
appsettings.Development.json
appsettings.local.json
appsettings.secrets.json
frontend/.env.local

# ── Node / Next.js ──
node_modules/
.next/
*.tsbuildinfo
npm-debug.log*

# ── opencode (tooling) ──
.opencode/node_modules/

# ── Tests / coverage ──
coverage/
coverage.*

# ── Logs runtime (réinitialisés à chaque démarrage de l'API) ──
logs/
